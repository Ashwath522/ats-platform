# Used by docker-compose.yml. Copy to .env and fill in before running
# `docker compose up`.

JWT_SECRET_KEY=change-me-to-a-long-random-string
CORS_ORIGINS=http://localhost:8080
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
