Continue work on the ats-platform repo. Before writing any code, do a full
audit of the existing implementation and report back what you find. Do NOT
assume anything about the current auth/UI system — verify it by reading the
actual files. This is critical: reuse what exists, don't rebuild it.

## STEP 0 — AUDIT (report back before touching anything)

1. Read backend/app/auth.py fully. Confirm exactly how roles currently work
   (candidate/recruiter), how JWTs are issued, and how role-based route
   protection is enforced (_get_current_user_with_role etc.).
2. Read the admin endpoint code — confirm current state of the hardcoded
   admin123 fallback and GET-query-string credential issue flagged earlier.
   Has it been fixed yet? If not, this MUST be fixed as part of this work,
   not left as a TODO.
3. List every existing frontend page/route (landing page, candidate signup,
   candidate dashboard, recruiter dashboard, any admin UI) and identify:
   - Which shared components exist (buttons, forms, cards, nav)
   - What font(s) and design tokens/CSS variables are already in use
   - What the current login/signup flow actually looks like end-to-end
4. Check the database models (SQLModel) — what tables exist for users,
   companies, job descriptions, applications. Confirm whether a "pending
   request" concept already exists anywhere, or would be new.
5. Confirm whether email-sending (Nodemailer equivalent / SMTP) is already
   wired up anywhere in the backend, or if this would be new infrastructure.

Report all of this back before proceeding to STEP 1. If any of it is
ambiguous or missing, ask me rather than guessing.

## STEP 1 — THE FLOW TO BUILD (reusing existing auth/UI only)

Target flow:

1. Landing page (existing shared UI) — no change to visual identity, reuse
   existing fonts/components/design tokens found in the audit.
2. Default self-signup path → creates a `role=candidate` user via the
   EXISTING auth system (do not create a parallel auth mechanism).
3. New "I'm a recruiter" entry point on the landing page → a lightweight
   form (email + phone only) → does NOT create a user account. Instead it
   creates a row in a new `recruiter_requests` table:
   (id, name, email, phone, status[pending/approved/rejected], submitted_at)
4. Admin login — MUST use the existing role-based JWT system, role=admin,
   NOT a hardcoded password or separate ad-hoc mechanism. If no admin role
   exists yet in the users table/enum, add it consistently with how
   candidate/recruiter roles are already defined.
5. Admin dashboard: list pending recruiter_requests, Approve/Reject actions.
6. On Approve: auto-generate a password, create a `users` row with
   role=recruiter using the EXISTING user-creation logic (don't duplicate
   it), then email the credentials to the recruiter's address.
7. Recruiter logs in through the EXISTING login page/endpoint — no new
   login form — and is routed to the recruiter dashboard based on the role
   claim already present in the JWT.

## STEP 2 — CONSTRAINTS (to prevent scope drift / hallucination)

- Do not introduce a new frontend framework, component library, or CSS
  approach. Match the existing frontend's structure exactly.
- Do not create a second/parallel auth system "for admin" — admin is just
  a third role value on the same JWT/role infrastructure already in place.
- Fix the admin123 hardcoded fallback and GET-query-string credential issue
  as part of this work, since the new admin role built on top of a broken
  admin auth mechanism would inherit the same hole.
- If email sending isn't wired up yet, use a standard SMTP library
  consistent with the existing backend stack (FastAPI) — don't add an
  unrelated third-party service without asking me first.
- After each step, tell me what you built, what existing code you reused
  vs. what was new, and what you actually tested (not just "should work").
- Do not mark anything done without running it.

## STEP 3 — UX DETAILS

- Recruiter request form: 2 fields only (email, phone) — keep it minimal,
  matching existing form styling.
- Admin dashboard: simple pending-requests table/list with Approve/Reject
  buttons — reuse existing table/list components if the audit found any.
- Show clear status feedback: "Request submitted, you'll receive login
  details by email once approved" after a recruiter submits.

Work through Step 0 first and report back before writing any code.
