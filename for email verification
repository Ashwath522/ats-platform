Continue work on ats-platform. Add email delivery for: (1) OTP-based
signup verification, (2) forgot-password reset, (3) recruiter credential
delivery on admin approval. Do NOT use Resend — audit shows it requires a
verified domain to send to non-owner addresses, which breaks the flow for
real users.

STEP 0 — AUDIT FIRST
- Check if any email-sending code already exists in the backend (search
  for smtp, mail, resend, sendgrid references). Report what's there.
- Confirm current env var / config setup so I know how to plug in SMTP
  credentials without hardcoding secrets.

STEP 1 — IMPLEMENT SMTP (Gmail, using an App Password)
- Add a single reusable `send_email(to, subject, body)` utility using
  Python's smtplib or fastapi-mail, configured via env vars
  (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_APP_PASSWORD) — no secrets in code.
- Wire this into: OTP verification emails, password reset emails, and
  recruiter credential emails — reuse the same utility for all three,
  don't write three separate senders.

STEP 2 — OTP / TOKEN LOGIC
- Generate a 6-digit OTP (or a random token for reset links), store it
  with an expiry (10-15 min) against the relevant user/request row.
- Add validation endpoints that check the OTP/token against the stored
  value and expiry before allowing the action to proceed.
- Invalidate the OTP/token after successful use or expiry.

STEP 3 — DEMO-SAFE FALLBACK (important for live presentation)
- Add a DEV_MODE env flag. When true and SMTP fails or isn't configured,
  return the OTP/token directly in the API response (clearly marked
  dev-only) instead of failing silently, so the flow can still be
  demonstrated without a live mailbox dependency.
- Never enable this fallback by default — it must require the explicit
  env flag.

Report back what you built, what you tested with a real email, and
confirm the DEV_MODE fallback works when SMTP is deliberately disabled.
